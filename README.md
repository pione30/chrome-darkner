# chrome-darkner

  Darken current page.

## How to build

  Install dependencies:

  ```Shell
  npm install
  ```

  For development

  ```Shell
  npm run dev
  ```

  "dist" is the directory to load via chrome://extensions.

  For production

  ```Shell
  npm run prod
  ```
